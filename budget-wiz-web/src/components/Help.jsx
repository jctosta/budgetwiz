import React from 'react';
import PropTypes from 'prop-types';

const Help = props => {
    return (
        <div>
            <h1>Help</h1>
        </div>
    );
};

Help.propTypes = {
    
};

export default Help;